# Compose Templates

该目录保存跨实验可复用的容器配置说明。每个实验仍维护自己的 `compose.yaml`，只启动该实验真正需要的依赖。

约束：

- 数据库与中间件版本必须固定，不使用 `latest`。
- 密码从 `.env` 读取，仓库只提交 `.env.example`。
- 容器必须配置健康检查。
- 卷名带项目名前缀，避免实验间数据互相污染。
- README 必须给出启动、停止、清理和健康检查命令。

